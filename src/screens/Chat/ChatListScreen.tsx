// This screen is superseded by ConnectionsScreen in the Discover stack.
// Connections (formerly matches) are accessed via Discover → Connections.
// Keeping this file as a re-export redirect to avoid broken imports.
export { default } from '../Discover/ConnectionsScreen';
